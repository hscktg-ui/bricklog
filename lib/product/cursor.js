/**
 * 인터랙티브 커서 — app/globals.css 의 --briclog-cursor-interactive 와 동일
 * Windows에서 깜빡임이 남으면 globals.css 에서 url(...) 커서로 교체 가능
 */
export const BRICLOG_CURSOR_INTERACTIVE = "var(--briclog-cursor-interactive)";

/** 버튼에 다른 용도의 ::after 가 필요할 때만 사용 */
export const BRICLOG_NO_SLAB_CLASS = "briclog-no-slab";
